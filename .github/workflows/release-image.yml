name: release-image

on:
  push:
    tags: [ '[0-9]+.[0-9]+.[0-9]+' ]

env:
  GHCR_REPO: ghcr.io/${{ github.repository }}
  DOCKER_PUSH: true
  LOCAL_KIND: false

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.GHCR_REPO }}

      # Login solo se dobbiamo pushare
      - name: Login to GHCR
        if: env.DOCKER_PUSH == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          push: ${{ env.DOCKER_PUSH == 'true' }}
          load: ${{ env.LOCAL_KIND == 'true' }}
          no-cache: ${{ env.LOCAL_KIND == 'true' }}

          # Multi-arch su GitHub, single-arch in locale
          platforms: ${{ env.LOCAL_KIND == 'true' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}

          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

          build-args: |
            CACHEBUST=${{ github.sha }}
            COMMIT_HASH=${{ github.sha }}

          # Cache intelligente: usa cache GHA solo in release
          cache-from: ${{ env.LOCAL_KIND == 'true' && '' || 'type=gha' }}
          cache-to:   ${{ env.LOCAL_KIND == 'true' && '' || 'type=gha,mode=max' }}


